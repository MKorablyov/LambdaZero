




# def train_model(train_data, val_data, test_data):

    # val_loss, train_loss = model.train(train_data, val_data)      # full training many epochs
    # test_loss = model.predict(test_data)                          # one epoch

    # embeds, preds, labels = model.predict(train_data, return_embeddings=True)
    # return embeds, preds, labels


# BaseActiveLearner()
#
# class NoOpActiveLearner(BaseActiveLearner):
#

# ActiveLearner(config: num_experiments, model)
# d
#


# train_data, val_data, test_data = Data()